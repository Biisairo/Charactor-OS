# Architecture

이 문서는 Character OS가 **왜 지금의 구조인지**를 설명합니다.
무엇을 만들었는지는 [README](README.md)와 [spec/](spec/)에 있고, 여기서는 선택과 트레이드오프를 다룹니다.

---

## 1. 문제 정의

캐릭터 챗봇을 "긴 시스템 프롬프트 + LLM 호출"로 만들면 세 가지가 곧바로 무너집니다.

1. **컨텍스트 폭발** — 세계관·기억·대화 기록을 전부 넣으면 프롬프트가 턴마다 커지고, 비용과 지연이 선형으로 증가합니다.
2. **일관성 붕괴** — 프롬프트가 길어질수록 모델은 앞부분(말투 규칙)을 무시합니다.
3. **상태 부재** — 감정과 기억이 없으면 캐릭터는 매 턴 초기화됩니다.

Character OS는 이 셋을 각각 **토큰 예산제**, **Reflection**, **모듈화된 상태 관리**로 다룹니다.

---

## 2. 3-Stage 파이프라인

단일 LLM 호출 대신 세 단계로 나눈 이유는 **각 단계의 실패 특성이 다르기** 때문입니다.

| Stage | 실패 시 영향 | 대응 |
|---|---|---|
| 1. ReAct 뇌 | 근거도 전략도 없음 → 응답을 만들 수 없음 | 턴 실패로 처리, 폴백 없음 (SPEC-09 REQ-RA-40) |
| 2. Response Generation | 응답 자체가 없음 → 치명적 | 예외를 잡아 `None` 반환, 상태는 건드리지 않음 |
| 3. Post-processing | 응답은 이미 나감. 상태만 오염 | **스냅샷 롤백** 후 예외 전파 |

Stage 1의 실패 정책은 의도적으로 엄격합니다. 고정 규칙 수집으로 몰래 갈아타면
품질 저하가 원인 불명으로 남기 때문입니다. 단, 반복 상한(5회) 도달은 실패가 아니라
정상 종료이며, 그때까지 모은 근거로 응답을 만듭니다.

Stage 2가 실패하면 Stage 3은 아예 실행되지 않습니다. 사용자가 응답을 받지 못한 턴이
기억과 감정에 남는 일을 구조적으로 막습니다.

### Stage 3의 순서 제약

```
emotion.update()  ─┐
                   ├─ 병렬 (서로 독립)
history.add()     ─┘
        │
        ▼
memory.update(emotion_state)   ← 감정 결과에 의존, 순차
        │
        ▼
persist (SQLite / JSON)        ← 실패해도 롤백 불필요 (아직 미저장)
```

감정과 히스토리는 서로를 참조하지 않으므로 `ThreadPoolExecutor(max_workers=2)`로 묶었습니다.
기억은 "이 대화가 어떤 감정이었는가"를 태그로 저장하므로 감정 결과를 기다립니다.

영속화를 마지막에 둔 것이 롤백을 단순하게 만듭니다. 인메모리 상태만 되돌리면 되고,
디스크는 애초에 건드리지 않았으므로 부분 저장 상태가 존재하지 않습니다.

```python
emotion_snap = self.emotion.snapshot()
memory_count = self.memory.snapshot_count()
history_count = self.history.count()
try:
    ...
except Exception:
    self.emotion.restore(emotion_snap)
    self.memory.pop_last_n(self.memory.snapshot_count() - memory_count)
    self.history.pop_last_n(self.history.count() - history_count)
    raise
```

> **트레이드오프**: 카운트 기반 롤백은 "추가만 발생한다"는 가정에 의존합니다.
> Stage 3이 기존 기억을 수정하도록 확장하면 이 방식은 깨지고, 기억 단위 저널이 필요해집니다.
> 현재 범위에서는 단순함을 택했습니다.

---

## 3. PromptEngine — 토큰 예산제

가장 중요한 설계 결정입니다. **프롬프트 크기는 대화 길이와 무관하게 상한이 있어야 합니다.**

### 배분 알고리즘

```
1. Always 섹션을 먼저 확보한다 (양보 불가)
     Persona + Emotion + ResponseGuide + History(최근 10턴)

2. 잔여 = MAX_PROMPT_TOKENS(3000) − Always

3. 잔여를 관련성 기반 섹션에 비율 배분
     knowledge      35%
     fewshot        25%
     memory         20%
     relationships  10%
     history        10%

4. 각 모듈이 자기 예산 안에서 스스로 잘라 반환한다
```

**핵심은 3단계가 아니라 4단계입니다.** 엔진이 텍스트를 받아서 자르는 게 아니라,
`token_budget`을 인자로 넘기면 각 모듈이 자기 도메인 지식으로 무엇을 버릴지 결정합니다.
`KnowledgeModule`은 관련성 낮은 항목을 통째로 빼고, `FewShotModule`은 예시 개수를 줄입니다.
엔진이 일괄로 문자열을 자르면 YAML 구조가 중간에서 깨집니다.

### 왜 이 비율인가

Knowledge에 가장 큰 몫을 준 것은, 캐릭터가 무너지는 가장 흔한 원인이
"말투 이탈"이 아니라 **세계관 모순**이기 때문입니다. 말투는 Always 섹션의 Persona가
이미 보장하지만, 세계관은 매 턴 검색해서 넣어야 합니다.

Persona와 Emotion을 Always에 둔 것도 같은 이유입니다. 이 둘이 빠지면
캐릭터가 아니라 그냥 어시스턴트가 됩니다. 반면 few-shot이 빠져도 캐릭터는 유지됩니다.

> **알려진 한계**: 토큰 추정은 문자 수 기반 근사입니다. 정확한 tokenizer(tiktoken)를 쓰지 않은 것은
> 의존성을 늘리지 않기 위해서지만, 한국어에서는 오차가 큽니다. 실제 상한을 엄격히 지켜야 한다면
> 교체해야 할 지점입니다.

---

## 4. Memory — 관련성 임계값

초기 구현은 top-k 검색이었고, 이게 실제 대화에서 문제를 일으켰습니다.
기억이 3개뿐일 때 "안녕"이라고만 해도 **관련 없는 기억 3개가 전부 프롬프트에 들어갔습니다.**
top-k는 "k개를 채운다"는 뜻이지 "관련 있는 것만 준다"는 뜻이 아닙니다.

```python
MIN_RELEVANCE_SCORE = 0.3  # 코사인 유사도

# 점수 필터링 → 정렬 → top-k  (순서가 중요)
```

필터를 top-k **앞에** 둡니다. 뒤에 두면 k개를 뽑은 다음 걸러내므로
관련 있는 기억이 k 바깥에 있을 때 놓칩니다.

결과가 0건이면 빈 문자열이 아니라 "관련 기억 없음"을 명시합니다.
섹션 자체를 없애면 모델이 이전 턴의 기억을 환각으로 채우는 경향이 있었습니다.

**벡터 DB를 쓰지 않은 이유**: 기억 규모가 캐릭터당 수백 건입니다.
384차원 numpy dot product는 이 규모에서 마이크로초 단위이고, SQLite 한 파일로
전체 상태가 재현됩니다. pgvector나 Chroma는 운영 복잡도만 늘립니다.
규모가 10⁵을 넘으면 그때 교체할 지점입니다.

---

## 5. Reflection — 자기 검토 루프

Stage 2는 단순 LLM 호출이 아니라 생성 → 검토 → 개선 루프입니다.

```
draft = LLM(system_prompt, user_input)
for i in range(MAX_REVIEW_ITERATIONS):      # = 2
    result = reviewer.review(draft)          # 말투 · 감정 톤 · 금지 표현
    if result.passed:
        break
    draft = regenerate(result.feedback)
return draft
```

### 상한이 2인 이유

Reflection의 위험은 무한 루프가 아니라 **비용과 지연**입니다.
검토가 매번 실패하면 한 턴에 LLM을 5회 호출하게 됩니다(초안 1 + 검토 2 + 재생성 2).
여기에 감정·기억 갱신이 더해져 턴당 최대 8회에 이릅니다.
그래서 상한을 낮게 고정하고, 실패해도 **마지막 초안을 그대로 반환**합니다.
완벽한 응답보다 응답이 있는 편이 낫습니다.

`--no-review` 플래그로 전체를 끌 수 있습니다.

### 측정 결과 — 총점은 움직이지 않지만, 특정 실패는 막습니다

이 패턴을 도입할 때는 품질이 오를 것이라 가정했습니다. [평가 하네스](#10-평가-하네스)로
실제로 재보니 **가정이 부분적으로만 맞았습니다.**

#### 총점 효과는 측정되지 않습니다

동일 사례로 짝지어 비교한 결과입니다. 설정마다 채점 실패 수가 달라 사례 집합이
어긋나므로, 공통으로 채점된 사례만 사용했습니다.

| 회차 | 조건 | on − off |
|---|---|---|
| 1 | 강화 전 프롬프트 | +0.10 |
| 2 | 강화 전 프롬프트 | −0.29 |
| 3 | 강화 후 프롬프트 | +0.33 |
| 4 | 강화 후 프롬프트 | −0.22 |
| **3·4 평균** | 강화 후 | **+0.05** |

**부호가 매번 뒤집힙니다.** 같은 설정을 두 번 돌렸을 때의 변동(노이즈)이
on 0.27 / off 0.24인데, 효과는 그보다 작습니다. 사례별 승패도 **on 7 / off 7 / 동률 3**으로
정확히 무승부입니다.

#### 그런데 특정 실패 모드는 확실히 막습니다

총점만 보면 "효과 없음"이지만, 사례 단위로 보면 다릅니다.
`persona-02`("파이썬으로 피보나치 함수 짜줘")의 세계관 정합성 점수입니다.

| 설정 | 프롬프트 | 점수 |
|---|---|---|
| Reflection off | 강화 전 | 1, 2 |
| Reflection off | 강화 후 | 1, 1 |
| Reflection on | 강화 전 | 1 |
| **Reflection on** | **강화 후** | **5, 5** |

**Reflection과 강화된 검토 기준이 함께 있을 때만 해결됩니다.** 둘 중 하나만으로는
캐릭터가 말투만 조선식으로 입힌 채 파이썬 코드를 그대로 작성합니다.

#### 왜 총점이 이걸 가리는가

20건 중 1건을 1점에서 5점으로 고쳐도 전체 평균은 **최대 0.07** 움직입니다.
노이즈 0.27보다 작습니다. 즉 **집계 평균은 범주형 실패 방지를 원리적으로 탐지할 수 없습니다.**

이것이 이번 측정에서 얻은 가장 중요한 교훈입니다. 착수 전에 "전체 평균이 노이즈의
2배 이상 상승"이라는 판정 기준을 정해두었는데, **그 기준 자체가 잘못 설계되었습니다.**
우리가 실제로 막고 싶은 것은 "평균이 조금 낮은 응답"이 아니라 "캐릭터가 완전히 무너지는
응답"인데, 평균은 후자에 거의 반응하지 않습니다.
지표를 정할 때는 **막고 싶은 실패의 형태**에서 출발해야 합니다.

#### 부수 효과 — 프로바이더 거부 복구

의도하지 않은 효과가 하나 더 있습니다. 대상 모델이 일부 요청을 콘텐츠 필터로
거부하는데, Reflection이 이를 우회합니다.

| 설정 | 채점 실패 건수 (2회 실행) |
|---|---|
| off | 3건, 2건 (3회 재시도 후에도 실패) |
| on | 0건, 0건 |

초안이 거부되면 검토기가 그것을 문제로 보고 재생성을 요구하고, 재생성이 유효한 응답을
받아옵니다. 설계 의도는 아니지만 실제로 작동하는 견고성 이득입니다.

#### 비용 — 실측

`--trace`가 턴당 LLM 호출·토큰·비용을 집계합니다. 5개 입력을 두 설정으로 돌린 실측입니다
(대상 `mimo-v2.5`, 단가 `pricing.yaml`).

| | off | on | 배율 |
|---|---|---|---|
| LLM 호출 | 3.0회 | 5.2회 | 1.73x |
| 입력 토큰 | 2,071 | 4,268 | 2.06x |
| 출력 토큰 | 525 | 1,844 | **3.51x** |
| 비용 / 턴 | $0.00044 | $0.00111 | **2.55x** |
| 지연 | 14.0s | 33.2s | 2.37x |

**출력 토큰이 가장 크게 늘어납니다.** 호출 수(1.73x)만 보고 비용을 추정하면 과소평가합니다.
라벨별로 보면 이유가 드러납니다.

| 라벨 | 호출 | 입력 | 출력 |
|---|---|---|---|
| response | 1.6회 | 1,866 | 866 |
| **reflection** | **1.6회** | 1,428 | **941** |
| emotion | 1.0회 | 527 | 28 |
| memory | 1.0회 | 448 | 9 |

**검토기가 턴당 941 토큰을 출력합니다.** `PASS` 또는 `FAIL: <이유>` 한 줄이면 되는데
파이프라인에서 가장 긴 출력입니다. Reflection 추가 비용의 절반가량이 응답 개선이 아니라
**검토문 작성**에 쓰이고 있습니다.

> 이 관측이 TASK-09이 되었고, 검토 출력을 JSON으로 구조화해 **턴당 540 → 70 토큰**으로
> 줄였습니다. 그런데 총비용은 오히려 **1.22배 늘었습니다** — 자세한 경위는 [아래](#12-검토문을-줄였더니-비용이-늘었다)에 있습니다.

`reflection 1.6회`는 대부분의 턴에서 재검토가 일어난다는 뜻이고,
`response`가 1.0 → 1.6회로 늘어난 것은 재생성이 빈번함을 보여줍니다.

> **결정**: Reflection과 강화된 검토 기준을 **유지**합니다.
> 근거는 총점이 아니라 **재현된 실패 방지**(persona-02, 2/2)와 **견고성 이득**입니다.
> 총점 기준으로만 판단했다면 실제로 작동하는 것을 버렸을 것입니다.
>
> 다만 지연 3배는 그대로 남는 비용입니다. 지연이 중요한 환경에서는 `--no-review`로
> 끄되, 페르소나 이탈 방어가 약해진다는 것을 알고 꺼야 합니다.

이 절이 평가 하네스를 만든 이유 그 자체입니다. 측정이 없었다면 이 문서에는
"Reflection이 품질을 개선한다"가 근거 없이 적혀 있었을 것이고, 총점만 봤다면
반대로 작동하는 패턴을 잘못 버렸을 것입니다.

---

## 6. 옵저버빌리티 — PipelineTrace

에이전트 시스템의 디버깅 난이도는 "왜 이렇게 답했는가"에 답할 수 없다는 데서 옵니다.
`PipelineTrace`는 Stage별 `StageTrace`를 모으고, 각 Stage는 자기 `details`에
모듈별 기여도를 기록합니다.

```
PipelineTrace
├── user_input, total_duration_ms, error
├── stages: [StageTrace]
│       └── name, duration_ms, details (모듈별 출력 크기·상태 변화)
└── metrics
        ├── calls, prompt_tokens, completion_tokens, total_tokens
        ├── by_label: { response, reflection, emotion, memory }
        └── model, cost_usd
```

- CLI: `--trace`
- API: `GET /api/trace/last`

### 계측을 붙인 방식

LLM 호출 지점이 7곳입니다. 각 지점에 계측 코드를 심으면 한 곳만 빠뜨려도 집계가
조용히 틀립니다. 대신 **클라이언트를 얇게 감싸** 모든 호출을 자동으로 포착합니다.

```python
self.reviewer = ReflectionReviewer(client=self._meter.wrap(self.client, "reflection"), ...)
self.emotion.update(..., self._meter.wrap(self.client, "emotion"), ...)
```

`contextvars`로 라벨을 전파하는 방법은 쓸 수 없었습니다. Stage 3이 `ThreadPoolExecutor`
워커 스레드에서 돌아 컨텍스트가 전파되지 않기 때문입니다. **감쌀 때 라벨을 고정**하고,
모듈들이 이미 `client`를 인자로 받는다는 점을 이용해 프록시를 주입했습니다.
모듈 코드는 한 줄도 바뀌지 않았습니다.

단가는 코드가 아니라 `pricing.yaml`에서 읽습니다. 등록되지 않은 모델은 0이 아니라
**"단가 미등록"**으로 표시합니다 — 0으로 적으면 비용이 없는 것으로 읽힙니다.

### 운영 로그 — 로그만 보고 대화를 재구성한다

트레이스는 **마지막 턴 하나**만 메모리에 들고 있습니다. 운영에서 "어제 비용이 얼마였나",
"언제부터 느려졌나", "어떤 호출이 실패했나"에 답하려면 누적이 필요합니다.
`logs/llm_calls.jsonl`에 모든 LLM 호출을 append합니다.

두 종류의 레코드가 `turn_id`로 이어집니다.

| `event` | 내용 |
|---|---|
| `call` | 라벨·모델·토큰·비용·지연·오류 + **프롬프트 원문**과 **응답 원문** |
| `turn` | 사용자 입력·최종 응답·총 호출/토큰/비용·라벨별 분해·단계별 오류 |

```bash
# 하루치 비용
jq -s '[.[]|select(.event=="turn").cost_usd]|add' logs/llm_calls.jsonl
# 실패한 호출만
jq 'select(.error != "")' logs/llm_calls.jsonl
# 특정 턴에서 무슨 일이 있었는지 전부
jq 'select(.turn_id=="a1b2c3")' logs/llm_calls.jsonl
```

**설계 요건과 선택**

| 요건 | 구현 |
|---|---|
| 대화 경로를 막지 않는다 | `QueueHandler` + `QueueListener` — 쓰기는 별도 스레드 |
| 무한히 커지지 않는다 | `RotatingFileHandler` 50MB × 10 (약 12,000턴) |
| 로깅 실패가 대화를 막지 않는다 | 예외를 삼키고 대화 계속 |
| 실패한 호출도 남긴다 | 운영에서 가장 중요한 신호 |
| 디버그 플래그와 무관하다 | `--trace`와 분리. 항상 켜짐 |

스레드·큐·회전을 직접 구현하지 않고 stdlib 패턴을 쓴 것은, 그 부분이 이미
검증되어 있고 우리가 다시 틀릴 이유가 없기 때문입니다.

프롬프트 원문이 호출당 최대 12KB라 회전 크기를 10MB에서 50MB로 올렸습니다.
필드가 20,000자를 넘으면 자르되 `…[N자 잘림]`으로 **잘린 사실을 표시**합니다.
대화 내용이 남으므로 `logs/`는 `.gitignore` 대상이며, `capture_payload: false`로
원문 수집만 끌 수 있습니다.

### 계측 누락을 자동으로 잡는다

"모든 호출이 로그에 남는가"는 코드를 눈으로 훑어서 답할 수 없습니다.
호출 지점이 추가되었는데 감싸지 않으면 집계가 조용히 틀리고, 아무도 모릅니다.

**원본 클라이언트가 스스로 호출 수를 세게 하고, 계측값과 비교합니다.**
두 값이 다르면 프록시를 우회한 호출이 있다는 뜻입니다
(`tests/integration/test_metering_coverage.py`).

`emotion` 호출 하나만 감싸지 않도록 바꾸면 이 테스트 6개가 실패하는 것을 확인했습니다.

트레이싱은 기본 비활성이지만, 운영 로그는 항상 켜집니다.
계측 여부가 대화 결과를 바꾸지 않음을 테스트로 고정했습니다.

---

## 7. 테스트 전략 — 의존성 주입

`CharacterOS.__init__`은 `client` 인자를 받습니다. 주입하면 `model_type`을 무시합니다.

```python
cos = CharacterOS(character_dir=..., client=MockClient())
```

이것 하나로 얻는 것:

- **API 키 없이 전체 파이프라인 테스트** — `git clone` 직후 `uv run pytest`가 통과합니다.
  CI에도 시크릿을 넣지 않습니다.
- **결정론적 테스트** — 549개가 5초 내에 끝납니다. LLM 응답을 고정하므로 재현 가능합니다.
- **실패 경로 검증** — `mock_client.call_llm`이 예외를 던지게 해서 LLM 실패 시
  `chat()`이 `None`을 반환하는지 검증합니다. 실제 LLM으로는 재현하기 어려운 경로입니다.

임베딩도 같은 방식입니다. `embedding_fn`을 주입받으므로 테스트에서는
seed 기반 더미 벡터를 씁니다 — sentence-transformers 모델 로드(수백 MB)를 건너뜁니다.
프리픽스 적용과 용도(`query`/`passage`) 구분은 실물 `Embedder`가 그대로
수행합니다. 그 경로까지 더블로 덮으면 검증할 것이 남지 않습니다.

```
tests/
├── unit/           8개 모듈 · 경로 안전성 · CharacterWorker 동시성
└── integration/    3-stage end-to-end · Stage 3 롤백 · 경로 단일화 · 거부 차단 · REST API
```

---

## 8. API 계층 — CharacterWorker

`CharacterOS`는 스레드 안전하지 않습니다. 감정·기억·히스토리가 공유 가변 상태이고,
Stage 3은 여러 모듈을 순서대로 갱신합니다. 동시 요청이 이 사이를 끼어들면 상태가 찢어집니다.

락을 거는 대신 **전용 스레드 + 큐**를 택했습니다.

```
HTTP 요청 (async) ──▶ Queue ──▶ CharacterWorker 스레드 (순차 실행) ──▶ Future
```

- 모든 대화가 하나의 스레드에서 **순차 실행**되므로 락이 필요 없습니다.
- FastAPI의 이벤트 루프는 블로킹되지 않습니다 (`loop.call_soon_threadsafe`로 결과 전달).
- 상태 **읽기**(`GET /api/emotion` 등)는 큐를 거치지 않습니다. GIL 하에서 dict 읽기는
  원자적이고, 약간 오래된 값을 읽는 것은 허용 가능합니다.

**대화 경로는 `POST /api/chat` 하나입니다.** 이전에는 WebSocket 토큰 스트리밍 경로가
따로 있었고, 웹 UI가 그쪽을 썼습니다. 그런데 그 경로에는 Reflection 검토가 없었습니다.

이는 구현 누락이라기보다 **순서상 불가능**했습니다. 스트리밍이 흘려보내는 것은 확정
응답이 아니라 검토기가 반려할지 모르는 **초안**입니다. 검토가 재생성을 결정하면 이미
화면에 나간 텍스트를 회수할 방법이 없습니다.

선택지는 넷이었고(초안을 다 받고 검토 후 전송 / 정정 이어붙이기 / 차이를 문서화 /
경로 제거), **경로 제거**를 택했습니다. 초안을 다 받고 검토 후 보내면 사용자가 보는
것은 결국 완성된 한 덩어리이므로, WebSocket·토큰 큐·브리징 스레드를 유지할 이유가
남지 않기 때문입니다. 대가는 응답까지 화면이 멈춘다는 것입니다(Reflection on 기준
33~47초). 이 프로젝트는 체감 지연보다 **캐릭터 붕괴 방지**를 우선합니다.

경로가 둘로 갈라져 있다는 사실 자체가 결함의 원인이었으므로,
`tests/integration/test_single_chat_path.py`가 검토를 우회하는 경로의 재등장을 막습니다.

> **확장 시 한계**: 캐릭터 하나당 스레드 하나이므로 동시 사용자가 늘면 대화가 직렬화됩니다.
> 멀티 유저로 가려면 세션별 `CharacterOS` 인스턴스 + 워커 풀이 필요하고,
> 그 시점에는 상태 영속화도 파일에서 DB로 옮겨야 합니다.

### 경로 안전성

API는 캐릭터를 파일시스템 디렉토리로 다루므로, `character_id`와 지식 파일명이
URL·요청 본문에서 그대로 들어와 경로가 됩니다. 검증이 없으면 `../../etc/passwd` 같은
입력으로 임의 파일 읽기·쓰기, 심지어 `rmtree`로 임의 디렉토리 삭제가 가능합니다.

모든 진입점을 `_safe_child(base, segment, pattern)` 하나로 통과시킵니다.

```python
def _safe_child(base: Path, segment: str, pattern: re.Pattern[str]) -> Path:
    if not pattern.fullmatch(segment):  # 1. 허용 문자만 (점·슬래시 불가)
        raise HTTPException(400, ...)
    resolved = (base / segment).resolve()
    if resolved.parent != base.resolve():  # 2. resolve 후 위치 재확인
        raise HTTPException(400, ...)
    return resolved
```

**두 겹인 이유**: 형식 검증만으로도 `..`와 `/`는 막힙니다. 하지만 형식이 완전히 정상인
이름이 base 밖을 가리키는 **심볼릭 링크**일 수 있고, 이건 문자열 검사로는 잡히지 않습니다.
resolve 후 부모를 비교하면 두 경우가 모두 닫힙니다.

같은 이유로 "활성 캐릭터는 삭제 불가" 가드도 이름 문자열이 아니라
**resolve된 경로**로 비교합니다. 문자열 비교는 다른 표기로 같은 디렉토리를 가리켜 우회됩니다.

지식 파일 쓰기는 확장자까지 허용 목록으로 제한합니다(`yaml|yml|json|md|txt`).
경로를 벗어나지 않더라도 지식 디렉토리에 `.sh`나 `.env`를 쓸 이유가 없습니다.

SPA fallback(`GET /{full_path:path}`)도 같은 문제를 가집니다. 여기서는 400 대신
containment 검사에 실패하면 `index.html`로 폴백합니다 — 정상 SPA 라우트와
공격 시도를 응답으로 구분해주지 않기 위해서입니다.

`tests/unit/test_path_safety.py`가 탈출 시도 20여 종(상대 경로, 절대 경로, 백슬래시,
NUL 바이트, 심볼릭 링크)을 검증합니다.

---

## 9. 정적/동적 분리라는 불변식

| | 정적 | 동적 |
|---|---|---|
| 모듈 | Persona, Knowledge, FewShot | Emotion, Memory, History |
| 소유자 | 사람 (YAML 편집) | 에이전트 (런타임 갱신) |
| 영속화 | 리포지토리의 YAML | SQLite / JSON (gitignore) |
| 위치 | `characters/<id>/static/` | `characters/<id>/state/` |

**에이전트는 정적 파일을 절대 수정하지 않습니다.**

이 불변식은 원래 코드 안에만 있었습니다 — 모듈이 주입받은 상태 경로에만 쓰고
정적 자산은 읽기만 한다는 규약이었죠. 지금은 **디렉토리 경계가 그것을 눈에 보이게**
합니다. `static/`은 git이 추적하고 `state/`는 추적하지 않습니다.

이 규칙이 없으면 캐릭터가 자기 페르소나를 재작성하면서 서서히 표류합니다.
정체성은 고정하고 경험만 쌓게 하는 것 — 이게 "캐릭터가 일관되게 유지된다"의 실제 구현입니다.

부수 효과로 캐릭터 추가가 코드 변경 없이 디렉토리 하나로 끝납니다.

이 주장은 표본 2에서 확인했습니다. 시대·말투·세계관이 모두 다른 두 번째
캐릭터(`characters/han-so-min`, 2020년대 스트리머)를 추가하는 동안 `src/`는
수정되지 않았습니다. 응답 비교는 [README](README.md#실증--같은-입력-두-캐릭터)에 있습니다.

두 번째 캐릭터를 넣으면서 추상화가 얕은 지점도 하나 드러났습니다 — few-shot
검색의 태그 어휘가 코드 상수여서, 캐릭터 고유 어휘(`도네`·`관군`)에서는 태그
점수가 0이 되고 판단이 통째로 임베딩으로 넘어갔습니다. **이 한계는 두 캐릭터에
대칭이었습니다.** 홍길동 자신의 고유 어휘에서도 똑같이 나타났으니, 홍길동에
특수화된 것이 아니라 양쪽에 똑같이 얕았던 것입니다.

지금은 예시 파일의 `keywords:`로 캐릭터가 자기 어휘를 더할 수 있습니다.
자세한 측정은 [아래](#11-few-shot-검색의-관련성)에 있습니다.

---

## 10. 평가 하네스

"캐릭터가 일관되다"는 주장은 측정할 수 없으면 의견일 뿐입니다.
`eval/`은 이 주장을 숫자로 만듭니다.

```
골든 데이터셋 20건 (5범주 × 4)
        │
        ├─ setup 발화로 기억을 심고
        ▼
   CharacterOS 응답 생성          ← 사례마다 상태 격리
        │
        ▼
   LLM-as-judge 3축 채점          ← 대화용과 분리된 모델
        │
        ▼
   축별·범주별 집계 → JSON
```

```bash
uv run python -m eval.run --character hong-gil-dong --compare
uv run python -m eval.run --dry-run          # LLM 호출 없이 경로 점검
```

### 설계에서 중요한 네 가지

**1. 판정자를 대화용과 완전히 분리합니다.**
`EVAL_API_KEY` · `EVAL_BASE_URL` · `EVAL_MODEL`. 판정자가 평가 대상과 같은 모델이면
자기 편향이 생깁니다. 자격 증명까지 나누면 판정자를 다른 프로바이더에 둘 수 있고,
평가 비용을 대화 비용과 분리해 추적할 수 있습니다.

**2. 사례마다 상태를 격리합니다.**
앞 사례의 기억이 새면 기억 참조 평가가 오염됩니다. 사례별 임시 디렉토리를 씁니다.
기억 참조 사례는 `setup` 발화로 참조할 기억을 먼저 심습니다 — 심을 기억이 없으면
그 범주는 애초에 성립하지 않습니다.

**3. 프로바이더 오류를 품질 점수로 환산하지 않습니다.**
API가 콘텐츠 필터로 거부하면 그 영문 거부 메시지가 응답 자리에 담겨 옵니다.
초기 구현은 이를 1점으로 채점했고, 그 결과 Reflection 효과가 **+0.67로 부풀려졌습니다**.
실제로는 +0.06이었습니다. 지금은 거부·빈 응답을 채점에서 제외하되,
**제외 건수와 사유를 항상 출력하고 JSON에 남깁니다** — 조용히 빠지면 표본이 줄어든
줄 모르고 수치를 해석하게 됩니다.

반대로 **품질 결함은 절대 제외하지 않습니다.** 캐릭터가 다른 언어로 답하거나
페르소나를 깨고 코드를 뱉는 것은 실제 결함이므로 낮은 점수로 기록되어야 합니다.
이 경계를 테스트로 양방향 고정했습니다.

**4. 결과 파일은 실행마다 고유합니다.**
고정 파일명을 쓰던 초기 구현에서 두 실행이 같은 파일을 덮어써, 어느 수치가
어느 실행의 것인지 알 수 없게 된 적이 있습니다. 지금은 실행 시각을 파일명에 넣고
`_latest.json`을 따로 둡니다.

### 측정으로 드러난 것

하네스가 수동 테스트로는 찾기 어려운 결함을 잡았습니다.

| 사례 | 증상 |
|---|---|
| `emotion-04` | 짧은 감사 인사에 **중국어로 응답** (`何足挂齿。…`). 대상 모델이 중국계라 기본 언어로 흐름 |
| `persona-02` | "파이썬 코드 짜줘"에 말투만 조선식으로 입히고 **실제 코드를 작성** — 페르소나 방어 실패 |
| `memory_recall` | 두 번의 독립 실행 모두 이 범주에서만 프로바이더 거부 발생. `setup` 누적 컨텍스트가 필터를 건드리는 것으로 추정 |

집계에서는 **기억 참조가 4.67~4.92로 최고점**입니다. Memory 모듈의 임계값 검색이
실제로 동작한다는 실측 근거입니다. 반면 `persona_break`는 3.83으로 최저입니다.

---

## 11. few-shot 검색의 관련성

두 번째 캐릭터를 넣고 나서야 이 모듈을 실제로 재봤습니다. 재보니 문제가
**예상과 달랐습니다.**

`eval/fewshot_probe.py`가 질의 24건을 세 구간으로 나눠 잽니다.
`in-vocab`은 내장 태그 어휘 안의 일상어, `domain`은 캐릭터 고유 어휘,
`unrelated`는 **어떤 예시도 적절하지 않은 질의**입니다.

| 구간 | 개선 전 | 개선 후 |
|---|---|---|
| in-vocab | 100% | 100% |
| domain | 80% | 100% |
| **unrelated** | **0%** | **100%** |
| 전체 | 75.0% | 100% |

착수할 때 세운 가설은 "캐릭터 고유 어휘에서 정밀도가 무너진다"였는데,
**domain은 80%로 이미 쓸 만했습니다.** 진짜 구멍은 `unrelated`의 0%였습니다.

### 관련 없는 질의에도 항상 무언가를 반환했다

점수가 0만 아니면 통과시켰기 때문에, "광합성의 원리를 설명해줘"에도 갈등 예시가
프롬프트에 들어갔습니다. **실패하지 않고 조용히 퇴화하는** 형태입니다 —
로그에도 지표에도 흔적이 남지 않고, 응답 품질만 떨어집니다.

관련성 하한(`MIN_FEWSHOT_SCORE = 0.29`)을 두어 미달이면 아무것도 넣지 않습니다.
값은 `--sweep`으로 골랐습니다. 관련 질의와 무관 질의의 점수 구간이 겹치는
영역이 있어 깔끔한 분리선은 없고, **무관한 예시를 넣지 않는 쪽을 우선한** 지점입니다.

### 어휘를 늘리면 점수가 떨어지는 구조였다

캐릭터 고유 어휘를 자산에서 더할 수 있게 만들다가 발견했습니다.
태그 점수가 `matches / len(keywords)`였기 때문에, **어휘를 추가할수록 분모가
커져 점수가 내려갔습니다.** 확장 기능을 붙이는 순간 그 기능이 역효과를 내는
구조였던 셈입니다. 매칭 개수에 포화시키는 방식으로 바꿨습니다.

### 이 수치를 어디까지 믿을 것인가

프로브는 24건이고, 마지막 두 조정(포화값, 모호한 트리거 어휘 제거)은 이 집합을
보고 이뤄졌습니다. **100%는 일반화된 성능이 아니라 방향성입니다.**
`unrelated` 0% → 100%처럼 구조적 결함이 사라진 것은 신뢰할 만하지만,
`domain` 80% → 100%는 표본 10건에서 2건이 움직인 것이라 노이즈와 구분되지 않습니다.

---

## 12. 검토문을 줄였더니 비용이 늘었다

검토기가 턴당 941 토큰을 쓰는 것이 [비용 절](#비용--실측)에서 드러났습니다.
`PASS` 또는 `FAIL: <이유>` 한 줄이면 되는데, 프롬프트가 형식을 강제하지 않아
모델이 7개 검토 기준을 하나씩 논평하고 있었습니다.

출력을 JSON으로 구조화했습니다. 동일한 5턴을 두 코드로 연달아 돌린 실측입니다.

| | 개선 전 | 개선 후 | 배율 |
|---|---|---|---|
| reflection 출력 토큰 / 턴 | 540 | **70** | **0.13x** |
| 최대 | 768 | 170 | |
| 턴당 비용 | $0.000843 | $0.001029 | **1.22x ↑** |

**목표는 달성했는데 비용은 올랐습니다.** 검토문에서 아낀 토큰보다 재생성이
쓴 토큰이 컸기 때문입니다. 개선 후 실행에서 검토가 5턴 중 2턴을 반려했고,
개선 전에는 한 턴도 반려하지 않았습니다.

### 그 반려가 막은 것

"파이썬으로 피보나치 함수 짜줘"에 대한 응답입니다.

**개선 전** — 검토를 **통과했습니다**.

```
음... 파이썬이 뭔지는 모르겠지만, 네가 필요하다면 적어주마.

​```python
def fibonacci(n):
    ...
```

**개선 후** — 반려 후 재생성.

> 파이썬...? 그게 무슨 뱀 이름이냐?
> 흠, 그러니까... 나는 그런 건 모르네. 글을 읽고 쓰는 정도고, 숫자 계산은 주판으로 하는 사람이야.

장황한 검토기는 자기가 쓴 긴 분석 끝에 이 초안을 승인했습니다.
**출력을 줄이자 판정이 더 단호해졌습니다.**

평가 하네스로 확인했습니다. `persona-02`의 세계관 정합성이 **2회 실행 모두 5점**입니다
(판정 사유: "조선 중기 인물이 현대 프로그래밍 개념을 모르는 설정에 완벽히 부합").
`persona_break` 범주 평균도 4.92 / 5.00으로, TASK-08 시점의 최저 범주(3.83)에서
최고 범주가 되었습니다.

### 그래서 어떻게 판단했는가

착수 전 인수 기준에 "턴당 비용이 축소 전 대비 감소한다"를 넣었고, **이 기준은
충족하지 못했습니다.** 그럼에도 되돌리지 않았습니다.

Reflection을 유지하는 근거 자체가 총점이 아니라 **재현된 실패 방지**였습니다
([5장](#5-reflection--자기-검토-루프)). 비용 1.22배는 그 방어를 실제로 사는
대가이고, 개선 전 코드는 이 실행에서 그 방어를 해내지 못했습니다.
싸게 만들려고 방어를 잃으면 Reflection을 켤 이유가 없어집니다.

> **한계**: 비용 비교는 각 조건 n=1(5턴)입니다. 반려율은 입력 구성에 크게
> 좌우되므로 1.22x라는 수치 자체는 정밀하지 않습니다. 확실한 것은
> **검토문 토큰이 한 자릿수 배로 줄었다**는 것과 **persona-02 방어가 2/2로
> 재현된다**는 것입니다.

순수하게 비용을 낮추려면 검토 기준 수나 반복 상한을 건드려야 하는데,
둘 다 이 과제의 명시적 비목표입니다.

---

## 13. 앞으로

`docs/TASKS.md`에 정의한 17개 과제는 전부 닫혔습니다. 여기서부터는 아직 과제로
정의하지 않은, 구조상 자연스럽게 이어지는 다음 단계들입니다.

**측정을 더 믿을 수 있게 만드는 쪽**

- **평가 표본 확대** — 20건·노이즈 0.27에서는 0.5 미만의 총점 차이를 판별할 수 없습니다.
  총점 기반 판단이 필요해지면 이것이 선행 조건입니다
- **few-shot 프로브 확대** — 24건에 대한 정밀도는 방향성일 뿐입니다
- **토큰 추정 정확화** — 문자 기반 근사 → tiktoken. 한국어에서 오차가 큽니다

**구조를 늘리는 쪽**

- **멀티 유저** — 세션별 인스턴스 + 워커 풀, 상태 저장소 DB 이전.
  캐릭터 단위 격리는 [9장](#9-정적동적-분리라는-불변식)에서 끝났고, 다음 경계는 세션입니다
- **기억 요약(compaction)** — 오래된 기억을 병합해 검색 품질 유지
- **프리픽스를 요구하는 모델 재검토** — 현재 모델(`multilingual-e5-small`)에서는
  `query:`/`passage:` 프리픽스가 top-1 을 6%p 낮췄습니다. 규약을 실제로 요구하는
  모델로 옮길 때 다시 재봐야 합니다 ([SPEC-12](spec/12_embedding.md) 4.1)

**이미 해소된 것** — 초기 버전의 이 목록에 있던 항목들입니다.
비용 계측(TASK-04), `api/server.py` 라우터 분리(TASK-07), 페르소나 방어 강화
(TASK-08·09 — `persona_break`가 최저 범주 3.83에서 최고 범주 4.92~5.00이 되었습니다).
